package Network.SOCKET;
import java.io.*;
import java.net.ServerSocket;
import java.net.Socket;

public interface SocketClient{
    //int portNumber=0;
    //Socket soc=new Socket("localhost", portNumber); //invia una richiesta di connessione al server
    //BufferedReader userInput=new BufferedReader(new InputStreamReader(System.in)); //legge da tastiera
}
